indexer
{
  mem_limit = 20M
}

searchd
{
  listen = 127.0.0.1:9312
  log = /home/asit/Apps/pj/log/searchd.log
  query_log = /home/asit/Apps/pj/log/searchd.query.log
  pid_file = /home/asit/Apps/pj/log/searchd.development.pid
}

source article_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = root
  sql_pass = test
  sql_db = pj
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT SQL_NO_CACHE `articles`.`id` * 2 + 0 AS `id` , `users`.`username` AS `author`, `articles`.`subject` AS `subject`, `articles`.`content` AS `content`, `article_tags`.`tag_name` AS `article_tag`, `articles`.`city` AS `city`, `articles`.`id` AS `sphinx_internal_id`, 3448190970 AS `class_crc`, '3448190970' AS `subclass_crcs`, 0 AS `sphinx_deleted`, CRC32(`article_tags`.`tag_name`) AS `article_tag_facet`, CRC32(`articles`.`city`) AS `city_facet`, UNIX_TIMESTAMP(`articles`.`created_at`) AS `created_at`, UNIX_TIMESTAMP(`articles`.`updated_at`) AS `updated_at`, `articles`.`article_tag_id` AS `article_tag_id`, `users`.`id` AS `author`, RADIANS(lat) AS `lat`, RADIANS(lng) AS `lng` FROM `articles`    LEFT OUTER JOIN `users` ON `users`.id = `articles`.user_id   LEFT OUTER JOIN `article_tags` ON `article_tags`.id = `articles`.article_tag_id  WHERE `articles`.`id` >= $start AND `articles`.`id` <= $end GROUP BY `articles`.`id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`id`), 1), IFNULL(MAX(`id`), 1) FROM `articles` 
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_uint = article_tag_facet
  sql_attr_uint = city_facet
  sql_attr_uint = article_tag_id
  sql_attr_uint = author
  sql_attr_timestamp = created_at
  sql_attr_timestamp = updated_at
  sql_attr_float = lat
  sql_attr_float = lng
  sql_attr_multi = uint subclass_crcs from field
  sql_query_info = SELECT * FROM `articles` WHERE `id` = (($id - 0) / 2)
}

index article_core
{
  source = article_core_0
  path = /home/asit/Apps/pj/db/sphinx/development/article_core
  charset_type = utf-8
  enable_star = 1
}

index article
{
  type = distributed
  local = article_core
}

source merchant_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = root
  sql_pass = test
  sql_db = pj
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT SQL_NO_CACHE `merchants`.`id` * 2 + 1 AS `id` , `merchants`.`name` AS `name`, GROUP_CONCAT(DISTINCT IFNULL(`loyalty_benefits`.`description`, '0') SEPARATOR ' ') AS `ben_desc`, GROUP_CONCAT(DISTINCT IFNULL(`loyalty_benefits`.`red_desc`, '0') SEPARATOR ' ') AS `ben_red_desc`, `merchant_categories`.`category_name` AS `category`, `merchants`.`description` AS `description`, `merchants`.`city` AS `city`, `merchants`.`state` AS `state`, `merchants`.`country` AS `country`, GROUP_CONCAT(DISTINCT IFNULL(`gcertsteps`.`step`, '0') SEPARATOR ' ') AS `step`, `merchants`.`id` AS `sphinx_internal_id`, 2380089271 AS `class_crc`, '2380089271' AS `subclass_crcs`, 0 AS `sphinx_deleted`, IFNULL(`merchants`.`name`, '') AS `name_sort`, CRC32(`merchant_categories`.`category_name`) AS `category_facet`, CRC32(`merchants`.`city`) AS `city_facet`, UNIX_TIMESTAMP(`merchants`.`created_at`) AS `created_at`, UNIX_TIMESTAMP(`merchants`.`updated_at`) AS `updated_at`, GROUP_CONCAT(DISTINCT IFNULL(`gcertificates`.`total_score`, '0') SEPARATOR ',') AS `eco_meter`, RADIANS(lat) AS `lat`, RADIANS(lng) AS `lng` FROM `merchants`    LEFT OUTER JOIN `loyalty_benefits` ON loyalty_benefits.merchant_id = merchants.id   LEFT OUTER JOIN `merchant_categories` ON `merchant_categories`.id = `merchants`.merchant_category_id   LEFT OUTER JOIN `gcertifications` ON gcertifications.merchant_id = merchants.id   LEFT OUTER JOIN `gcertsteps` ON `gcertsteps`.id = `gcertifications`.gcertstep_id   LEFT OUTER JOIN `gcertificates` ON gcertificates.merchant_id = merchants.id  WHERE `merchants`.`id` >= $start AND `merchants`.`id` <= $end GROUP BY `merchants`.`id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`id`), 1), IFNULL(MAX(`id`), 1) FROM `merchants` 
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_uint = category_facet
  sql_attr_uint = city_facet
  sql_attr_timestamp = created_at
  sql_attr_timestamp = updated_at
  sql_attr_str2ordinal = name_sort
  sql_attr_float = lat
  sql_attr_float = lng
  sql_attr_multi = uint subclass_crcs from field
  sql_attr_multi = uint eco_meter from field
  sql_query_info = SELECT * FROM `merchants` WHERE `id` = (($id - 1) / 2)
}

index merchant_core
{
  source = merchant_core_0
  path = /home/asit/Apps/pj/db/sphinx/development/merchant_core
  charset_type = utf-8
  enable_star = 1
}

index merchant
{
  type = distributed
  local = merchant_core
}
