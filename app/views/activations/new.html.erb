<h1><%= @user.username %> Activate Your Account</h1>
<% form_for @user, :url => activate_path(@user.id), :html => {:method => :post} do |f| %>
  <%= f.error_messages %>
  <%= f.submit "Activate" %>
<% end  %>
