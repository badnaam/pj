<script type="text/javascript">
  //<![CDATA[
  $j(document).ready(function() {
    var icons = {
      header: "ui-icon-circle-arrow-e",
      headerSelected: "ui-icon-circle-arrow-s"
    };

    $j("#intro_tabs").tabs(
    { fx: { opacity: 'toggle' }, event : 'mouseover'
    });

    $j('#intro_accordion').accordion(
    { event : 'mouseover', autoHeight : false, animated : 'bounceslide'}
  );
    $j('#intro_header').accordion({collapsible : true, active :false, icons : icons});
  });
  //]]>
</script>
<% page_title(t "txt.logo")  %>
<%= render :partial => "intro" %>
<%#= render :action => :search_get, :controller => :searches, :stype => Search::SEARCH_TYPE["all"], :layout => false %>
<%# @search = Search.new %>
<%  @search = init_search(Search::SEARCH_TYPE["all"])%>
<%= render :partial => "/searches/form", :object => @search, :locals => {:stype => Search::SEARCH_TYPE["all"], :show_refine => false}  %>

<%#= render :partial => "/searches/form", :locals => {:search => Search.new } %>
